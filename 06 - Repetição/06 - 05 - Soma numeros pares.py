#Crie um programa leia 6 numeros inteiros e que faça a soma de numeros pares
#se digitado numero impar, desconsiderar
from datetime import date
atual = date.today().year
s = 0
for c in range(1, 4):
    nasc = int(input('Digite o {}º valor par:'.format(c)))
    if

print(s)